//#include "interface.h"
//#include "dungeon.h"
//
//#define N_ROW 2
//
////int getnumbercolunm(std::vector<geometry::point> &dungeonfeature)
////{
////  // set dimension for output MATLAB array
////  int n_colunm = N_ROW * static_cast<int>(dungeonfeature.size());
////  return n_colunm;
////}
//
//void copyarray(std::vector<geometry::point> &dungeonpoint, double *vectorin, int size)
//{
//  for(int i = 0; i < size; i++)
//    {
//      int j = i / 2;
//      (i % 2 == 0) ? vectorin[i] = dungeonpoint.at(j).first : vectorin[i] = dungeonpoint.at(j).second;
//      qDebug() << vectorin[i];
//    }
//}

